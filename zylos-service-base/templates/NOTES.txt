zylos-service-base v{{ .Chart.Version }} — released as {{ .Release.Name }} in namespace {{ .Release.Namespace }}.

Image:        {{ include "service.image" . }}
Replicas:     {{- if .Values.autoscaling.enabled }} HPA-managed ({{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }}) {{- else }} {{ .Values.replicaCount }} {{- end }}
Service:      {{ include "service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.httpPort }}
Management:   port {{ .Values.service.managementPort }} (Spring Boot Actuator)
{{- if .Values.ingress.enabled }}
Ingress:
{{- range .Values.ingress.hosts }}
  - http://{{ .host }}/
{{- end }}
{{- end }}

Verify:
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "service.fullname" . }}
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}

Tail logs:
  kubectl -n {{ .Release.Namespace }} logs -l app.kubernetes.io/instance={{ .Release.Name }} -f --tail=100

Open Spring Boot Actuator:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "service.fullname" . }} 9000:{{ .Values.service.managementPort }}
  curl http://localhost:9000/actuator/health
